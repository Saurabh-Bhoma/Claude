Delegate this task to the @planner agent.

I want to implement: $ARGUMENTS

The planner agent has the new-endpoint skill and full VLMS codebase context. It will research the codebase, find reusable code, and produce a structured implementation plan.

After the planner returns its plan, present it to me and wait for approval before writing any code.
